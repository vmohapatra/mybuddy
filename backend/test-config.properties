# Test Configuration for Search Testing
# DuckDuckGo is enabled by default (no API key required)

# Search Configuration
search.duckduckgo.enabled=true

# Application Configuration
spring.profiles.active=dev
server.port=8080
